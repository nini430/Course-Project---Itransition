export interface FullNameSettings {
    firstName:string;
    lastName:string;
}


export interface EmailSettings {
    email:string;
}

export interface PasswordSettings {
    oldPassword:string;
    password:string;
    confirmPassword:string;
}